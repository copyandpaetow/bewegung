import { WorkerState } from "./types";

export const createKeyframes = (state: WorkerState): Map<string, Keyframe> => {
	const keyframes = new Map<string, Keyframe>();

	//complete readouts from either missing elements (diosplay none, remove) or added ones
	//filter elements that dont change or are completely hidden

	//seperate images and defaults
	//calculate easings
	//calculate keyframes

	return keyframes;
};
